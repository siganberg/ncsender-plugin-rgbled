Warning: no stdin data received in 3s, proceeding without it. If piping from a slow command, redirect stdin explicitly: < /dev/null to skip, or wait longer.
## What's Changed

### ✨ New Features
- New Colors tab for configuring LED color settings
- New Firmware tab for managing firmware options
- Added an idle-dim timeout setting so you can control how long the plugin waits before dimming the LEDs

### 🔧 Improvements
- Polished the configuration screen for a cleaner, easier-to-navigate layout
